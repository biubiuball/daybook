{
  "name": "daybook",
  "private": true,
  "type": "module",
  "scripts": {
    "build:fonts": "bash tools/font-subsets/build.sh",
    "render:katex": "node scripts/render-katex.mjs",
    "typecheck": "tsc --noEmit",
    "build:js": "node scripts/build-js.mjs",
    "watch:js": "node scripts/build-js.mjs --watch",
    "build": "npm run typecheck && npm run build:js && go run ./cmd/daybook build",
    "serve": "npm run typecheck && npm run build:js && concurrently \"npm run watch:js\" \"go run ./cmd/daybook serve\""
  },
  "dependencies": {
    "jsdom": "^29.1.1",
    "katex": "^0.16.10",
    "puppeteer": "^25.1.0"
  },
  "version": "1.0.0",
  "description": "A minimalist static blog generator for Go and HTML beginners.",
  "main": "patch.js",
  "directories": {
    "doc": "docs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/StatIndet/daybook.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/StatIndet/daybook/issues"
  },
  "homepage": "https://github.com/StatIndet/daybook#readme",
  "devDependencies": {
    "concurrently": "^10.0.3",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  }
}
